node_modules/
dist/
apps/web/server.js
.env
.env.local
.DS_Store

# Local Docker override (exposes DB port for dev tools — not for production)
docker-compose.override.yml

# Drizzle generated migrations (keep - version control desired)
# packages/db/drizzle/

# pnpm lock (keep for reproducibility)
# pnpm-lock.yaml

# Claude Code and GSD planning (local only)
.claude/
.planning/
