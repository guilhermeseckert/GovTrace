---
phase: 04.2-entity-profile-storytelling
plan: 01
subsystem: api
tags: [claude-api, drizzle, entity-connections, ai-summary]

requires:
  - phase: 02-search-and-entity-profiles
    provides: Entity profile pages, AI summary generation, ConnectionsTable
provides:
  - Shared CONNECTION_LABELS and formatAmount utility in lib/connection-labels.ts
  - Real connection count in getEntityStats (was hardcoded 0)
  - Enriched AI summary prompt with named entities, dollar amounts, and cross-dataset references
  - PROMPT_VERSION cache invalidation for old summaries
affects: [04.2-entity-profile-storytelling]

tech-stack:
  added: []
  patterns: [bidirectional entity_connections query for top connections, prompt versioning via model field suffix]

key-files:
  created:
    - apps/web/src/lib/connection-labels.ts
  modified:
    - apps/web/src/components/tables/ConnectionsTable.tsx
    - apps/web/src/server-fns/entity.ts
    - apps/web/src/server-fns/summary.ts

key-decisions:
  - "PROMPT_VERSION suffix on model field (-v2) invalidates old cached summaries without requiring a migration"
  - "getTopConnections queries both entityAId and entityBId directions then merges/sorts by totalValue"
  - "Word limit bumped from 60 to 80, max_tokens from 300 to 500 for richer cross-referenced summaries"

patterns-established:
  - "Shared connection-labels.ts: single source of truth for CONNECTION_LABELS and formatAmount"
  - "Prompt versioning: append version suffix to model field for cache invalidation"

requirements-completed: [STORY-01]

duration: 4min
completed: 2026-04-02
---

# Phase 04.2 Plan 01: Foundation Utilities Summary

**Shared connection-labels lib, fixed getEntityStats connection count, and enriched AI summary prompt with named entities, dollar amounts, and cross-dataset storytelling**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-02T16:37:22Z
- **Completed:** 2026-04-02T16:41:46Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Extracted CONNECTION_LABELS and formatAmount to shared lib for reuse by ConnectionCards (Plan 02)
- Fixed hardcoded `connections: 0` in getEntityStats to query real count from entity_connections table
- Rewrote buildSummaryPrompt to include top 20 connections with specific entity names and dollar amounts
- Fixed hardcoded `lobbyCount: 0` to query lobbyRegistrations table
- Added PROMPT_VERSION cache invalidation so old summaries regenerate with enriched content

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract shared connection-labels lib and fix getEntityStats** - `12e2f8d` (feat)
2. **Task 2: Upgrade buildSummaryPrompt with enriched connection data** - `9bfb66b` (feat)

## Files Created/Modified
- `apps/web/src/lib/connection-labels.ts` - Shared CONNECTION_LABELS map and formatAmount utility
- `apps/web/src/components/tables/ConnectionsTable.tsx` - Updated to import from shared lib
- `apps/web/src/server-fns/entity.ts` - Fixed getEntityStats with real connection count query
- `apps/web/src/server-fns/summary.ts` - Enriched AI prompt with named connections, fixed lobby count, added prompt versioning

## Decisions Made
- Used PROMPT_VERSION suffix on model field (`-v2`) to invalidate old cached summaries without requiring a database migration
- getTopConnections queries both directions of entity_connections and merges results sorted by totalValue descending
- Bumped word limit from 60 to 80 and max_tokens from 300 to 500 to accommodate richer cross-referenced summaries

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Shared lib ready for Plan 02 (ConnectionCards) to import CONNECTION_LABELS and formatAmount
- Enriched summary prompt ready to generate storytelling summaries on next entity page visit
- Old cached summaries will auto-regenerate with enriched content

---
*Phase: 04.2-entity-profile-storytelling*
*Completed: 2026-04-02*
