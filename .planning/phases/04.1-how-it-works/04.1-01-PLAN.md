---
phase: 04.1-how-it-works
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/how-it-works.tsx
  - apps/web/src/components/ui/accordion.tsx
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-02
  - ONBOARD-03
  - ONBOARD-04
  - ONBOARD-05

must_haves:
  truths:
    - "Page renders at /how-it-works with all 5 sections visible"
    - "Data Sources section lists all 5 government sources with links to .gc.ca portals"
    - "Step-by-step walkthrough shows 3 numbered steps with descriptions"
    - "AI transparency section explains confidence tiers and what AI does/doesn't do"
    - "FAQ section has collapsible accordion items for all required questions"
    - "Dark mode renders correctly with alternating section backgrounds"
  artifacts:
    - path: "apps/web/src/routes/how-it-works.tsx"
      provides: "Static how-it-works page with Hero, Data Sources, Walkthrough, AI Transparency, FAQ sections"
      min_lines: 150
    - path: "apps/web/src/components/ui/accordion.tsx"
      provides: "shadcn Accordion component for FAQ"
  key_links:
    - from: "apps/web/src/routes/how-it-works.tsx"
      to: "apps/web/src/components/ui/accordion.tsx"
      via: "import Accordion components"
      pattern: "from '@/components/ui/accordion'"
---

<objective>
Create the full "How It Works" static explainer page with all 5 content sections: hero intro, data sources, step-by-step walkthrough, AI transparency, and FAQ.

Purpose: First-time visitors need to understand GovTrace in under 60 seconds — what it does, where data comes from, how AI is used, and answers to common concerns.
Output: Complete /how-it-works route with accordion component installed.
</objective>

<execution_context>
@/Users/geckert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-how-it-works/04.1-CONTEXT.md

<interfaces>
<!-- Layout and navigation patterns from existing codebase -->

From apps/web/src/routes/__root.tsx:
```typescript
// NavLink component pattern (line 36-51)
function NavLink({ to, children }: { to: string; children: React.ReactNode })
// Section container: mx-auto max-w-7xl px-4
```

From apps/web/src/routes/index.tsx:
```typescript
// Route creation pattern
export const Route = createFileRoute('/')({ component: IndexPage })
// Section pattern: mx-auto max-w-7xl px-4 py-16
// Card containers: rounded-xl border bg-card p-5 shadow-sm
// Color-coded icon containers: bg-{color}-100 text-{color}-600 dark:bg-{color}-950 dark:text-{color}-400
```

From apps/web/src/components/entity/ConfidenceBadge.tsx:
```typescript
// Three confidence tiers:
// high: score >= 0.85 (green #16a34a, ShieldCheck icon)
// medium: score 0.6-0.85 (amber #d97706, Shield icon)
// low: score < 0.6 (red #dc2626, ShieldAlert icon)
```

From apps/web/src/components/entity/AISummaryExplanation.tsx:
```typescript
// Canonical AI explanation prose:
// - Summaries written by Claude using counts/categories from government records
// - AI given only statistical information
// - Does NOT contain: editorial judgments, allegations, speculation, non-government info
// - Caveat: "Connections shown do not imply wrongdoing"
```

From apps/web/src/app.css:
```css
/* Fonts: Instrument Serif (headings), DM Sans (body), JetBrains Mono (code) */
/* Brand tokens: --gt-navy, --gt-gold, --gt-red, --gt-teal */
/* font-serif maps to Instrument Serif */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Accordion and create How It Works page</name>
  <files>apps/web/src/components/ui/accordion.tsx, apps/web/src/routes/how-it-works.tsx</files>
  <read_first>
    - apps/web/src/routes/__root.tsx (NavLink pattern, container widths, font classes)
    - apps/web/src/routes/index.tsx (section layout pattern, card styling, icon color pattern)
    - apps/web/src/components/entity/AISummaryExplanation.tsx (canonical AI explanation text to reuse/expand)
    - apps/web/src/components/entity/ConfidenceBadge.tsx (confidence tier thresholds and colors)
    - apps/web/src/app.css (font families, brand tokens, dark mode variables)
  </read_first>
  <action>
**Step 1: Install shadcn Accordion**
Run `cd apps/web && npx shadcn@latest add accordion` to install the Accordion component.

**Step 2: Create /how-it-works route**
Create `apps/web/src/routes/how-it-works.tsx` as a static page (no loader, no server functions) per D-04.

Use `createFileRoute('/how-it-works')` pattern matching existing routes.

**Page structure** (per D-06, all sections stacked):

1. **Hero Section** (bg-background):
   - Centered layout with `font-serif text-4xl` heading: "How GovTrace Works"
   - Subtitle in muted-foreground: brief one-liner about tracing money in Canadian government
   - Keep it punchy — 1-2 sentences max

2. **Data Sources Section** (bg-muted/30 per D-07 alternating pattern):
   - Section heading: "Where the Data Comes From" (font-serif text-3xl, centered)
   - Grid of 5 source cards (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3) per D-08, D-10
   - Each card: `rounded-xl border bg-card p-5 shadow-sm` (matching index.tsx pattern)
   - Each card has: Lucide icon in color-coded container, source name, description of what data it contains, update frequency, external link to .gc.ca portal
   - Icons per D-09:
     - Elections Canada donations: `Vote` icon, blue color scheme
     - Federal contracts: `FileText` icon, emerald color scheme
     - Federal grants: `Gift` icon, amber color scheme
     - Lobbyist registrations: `Users` icon, purple color scheme
     - Lobbyist communications: `MessageSquare` icon, teal color scheme
   - Each link opens in new tab with rel="noopener noreferrer" and ExternalLink icon
   - Source URLs:
     - Elections Canada: https://www.elections.ca/content.aspx?section=fin&dir=oth&document=index&lang=e
     - Contracts: https://open.canada.ca/data/en/dataset/d8f85d91-7dec-4fd1-8055-483b77225d8b
     - Grants: https://open.canada.ca/data/en/dataset/432527ab-7aac-45b5-81d6-7b1a6f51e59b
     - Lobbying: https://lobbycanada.gc.ca/app/secure/ocl/lrs/do/guest

3. **Step-by-Step Walkthrough Section** (bg-background) per D-11, D-12, D-13:
   - Section heading: "How to Use GovTrace" (font-serif text-3xl, centered)
   - 3 steps laid out horizontally on desktop (flex-col md:flex-row), with large numbered circles (h-12 w-12 rounded-full bg-primary text-primary-foreground font-serif text-xl) and connecting dashed lines between them on desktop
   - Step 1: "Search a Name" — Type any name into the search bar: a politician, company, or person. GovTrace searches across all government datasets instantly.
   - Step 2: "See Their Connections" — View donations, contracts, grants, and lobbying activity all in one place. Every record links back to the original government source.
   - Step 3: "Understand the Money Trail" — Interactive visualizations show you the flow: who donated to whom, which companies got contracts, and how lobbying connects to spending.
   - Each step: number circle + bold title + description paragraph
   - Descriptions written at grade-school level per D-22

4. **AI Transparency Section** (bg-muted/30) per D-14, D-15, D-16, D-17:
   - Section heading: "How AI Helps (and What It Doesn't Do)" (font-serif text-3xl, centered)
   - Two-column layout on desktop (grid-cols-1 lg:grid-cols-2):
     - Left column "What AI Does": entity matching (finding the same person across datasets), summary generation (plain-language descriptions from statistics). Use check-circle icons (green).
     - Right column "What AI Doesn't Do": no accusations, no editorial judgment, no speculation, no information beyond government records. Use x-circle icons (red/muted).
   - Confidence tier visual per D-15, D-17:
     - Show 3 inline badge examples matching ConfidenceBadge colors:
       - High (green, ShieldCheck): "85%+ — Strong match across multiple datasets"
       - Medium (amber, Shield): "60-85% — Reviewed by AI with reasoning"
       - Low (red, ShieldAlert): "Below 60% — Uncertain, flagged for review"
     - Each as a styled inline example (not interactive Popover, just visual)
   - Reuse and expand prose from AISummaryExplanation.tsx per D-14
   - End with italic caveat: "Connections shown in GovTrace do not imply wrongdoing."

5. **FAQ Section** (bg-background) per D-18, D-19, D-20:
   - Section heading: "Frequently Asked Questions" (font-serif text-3xl, centered)
   - Use shadcn Accordion (type="single", collapsible) per D-18
   - Questions (per D-19, answers at grade-school level per D-20):
     - "Is this legal?" — Yes. All data comes from public government datasets published under the Open Government Licence — Canada. Anyone can access these records. GovTrace just makes it easier to search and connect them.
     - "Can I trust the AI?" — The AI only uses public government data to match names and write summaries. It never makes accusations or judgments. Every AI decision shows a confidence score and reasoning, so you can see exactly why it made a match. If something looks wrong, you can flag it.
     - "What does a connection mean?" — A connection means two names appear in related government records — for example, a person donated to a politician, or a company received a contract. It does not mean anything improper happened. Many connections are routine and expected.
     - "Where does the data come from?" — Five public federal datasets: Elections Canada (political donations), open.canada.ca (contracts and grants), and lobbycanada.gc.ca (lobbying registrations and communications). Each record links to the original government source.
     - "Who built this?" — GovTrace is an open-source civic technology project. The code is available on GitHub. It was built to make public data more accessible to everyone — journalists, researchers, and curious citizens.

6. **CTA Section** (bg-muted/30, optional — Claude's discretion):
   - Simple centered text with a Link to "/search": "Ready to explore? Try searching a name." with an ArrowRight icon
   - Uses Link component from @tanstack/react-router

**Styling rules:**
- Container: `mx-auto max-w-7xl px-4 py-16` per D-05
- Alternating backgrounds: odd sections bg-background, even sections bg-muted/30 per D-07
- Headings: `font-serif` (Instrument Serif) per project conventions
- Body text: default font (DM Sans)
- All prose inline in JSX per D-21 — do NOT add strings to en.ts
- Semantic HTML: use section elements, proper heading hierarchy (h1 for page, h2 for sections, h3 for sub-items)
- Accessible: all links have descriptive text, images have alt, proper ARIA where needed
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && pnpm exec -- ultracite check && ls apps/web/src/routes/how-it-works.tsx apps/web/src/components/ui/accordion.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/ui/accordion.tsx exists (shadcn installed)
    - apps/web/src/routes/how-it-works.tsx exists with createFileRoute('/how-it-works')
    - Page has all 5 sections: hero, data sources, walkthrough, AI transparency, FAQ
    - Data sources section has 5 cards with Lucide icons and .gc.ca links
    - Walkthrough has 3 numbered steps
    - AI transparency shows 3 confidence tiers with correct colors
    - FAQ uses Accordion component with 5 collapsible questions
    - Alternating section backgrounds (bg-background / bg-muted/30)
    - All prose is inline JSX (no en.ts additions)
    - No loader or server function imports
    - Passes ultracite check (lint + format)
  </acceptance_criteria>
  <done>Static how-it-works page renders all 5 content sections with proper styling, dark mode support, and semantic HTML</done>
</task>

</tasks>

<verification>
- `ls apps/web/src/routes/how-it-works.tsx` — file exists
- `ls apps/web/src/components/ui/accordion.tsx` — accordion installed
- `grep 'createFileRoute' apps/web/src/routes/how-it-works.tsx` — route registered
- `grep 'Accordion' apps/web/src/routes/how-it-works.tsx` — FAQ uses accordion
- `grep 'elections.ca' apps/web/src/routes/how-it-works.tsx` — data source links present
- `pnpm exec -- ultracite check` — passes lint and format
</verification>

<success_criteria>
The /how-it-works page exists with all required sections, passes lint, and is ready for nav wiring in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-how-it-works/04.1-01-SUMMARY.md`
</output>
