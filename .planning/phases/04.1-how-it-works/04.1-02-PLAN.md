---
phase: 04.1-how-it-works
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - apps/web/src/routes/__root.tsx
autonomous: false
requirements:
  - ONBOARD-01

must_haves:
  truths:
    - "Header nav shows 'How it works' link that navigates to /how-it-works"
    - "Footer shows 'How it works' link that navigates to /how-it-works"
    - "Nav link shows active state when on /how-it-works page"
    - "Page is visually correct with proper typography, spacing, dark mode, and responsive layout"
  artifacts:
    - path: "apps/web/src/routes/__root.tsx"
      provides: "Updated header and footer with How it works navigation links"
      contains: "how-it-works"
  key_links:
    - from: "apps/web/src/routes/__root.tsx"
      to: "apps/web/src/routes/how-it-works.tsx"
      via: "NavLink to='/how-it-works' in SiteHeader and Link in SiteFooter"
      pattern: "how-it-works"
---

<objective>
Wire "How it works" navigation links into the site header and footer, then verify the complete page visually.

Purpose: Users need to discover the How It Works page from both the header navigation and the footer links.
Output: Updated __root.tsx with nav links; human-verified page appearance.
</objective>

<execution_context>
@/Users/geckert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-how-it-works/04.1-CONTEXT.md
@.planning/phases/04.1-how-it-works/04.1-01-SUMMARY.md

<interfaces>
From apps/web/src/routes/__root.tsx:
```typescript
// SiteHeader nav section (line 68-74):
<div className="flex items-center gap-1">
  <NavLink to="/search">
    <Search className="h-3.5 w-3.5" />
    Search
  </NavLink>
  <ThemeToggle />
</div>

// SiteFooter links section (line 97-109):
<div className="space-y-2 text-xs text-muted-foreground">
  <p>All data under Open Government Licence — Canada</p>
  <p>Connections shown do not imply wrongdoing</p>
  <a href="https://github.com/govtrace" ...>Open source on GitHub</a>
</div>

// NavLink component (line 36-51) — handles active state styling
function NavLink({ to, children }: { to: string; children: React.ReactNode })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add How it works links to header and footer navigation</name>
  <files>apps/web/src/routes/__root.tsx</files>
  <read_first>
    - apps/web/src/routes/__root.tsx (current SiteHeader and SiteFooter implementation)
  </read_first>
  <action>
**Header (SiteHeader, per D-02, D-03):**
- Add `BookOpen` to the lucide-react import (or `HelpCircle` — use `BookOpen` for "learn" semantics)
- Add a new NavLink before the Search NavLink in the header nav:
  ```
  <NavLink to="/how-it-works">
    <BookOpen className="h-3.5 w-3.5" />
    How it works
  </NavLink>
  ```
- This follows the exact same pattern as the existing Search NavLink

**Footer (SiteFooter, per D-02, D-03):**
- In the right-side div (text-xs text-muted-foreground), add a Link to /how-it-works before the GitHub link:
  ```
  <Link to="/how-it-works" className="inline-flex items-center gap-1.5 text-muted-foreground transition-colors hover:text-foreground">
    How it works
  </Link>
  ```
- The Link import is already available from @tanstack/react-router

Run `pnpm exec -- ultracite fix` after editing.
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && pnpm exec -- ultracite check && grep -c "how-it-works" apps/web/src/routes/__root.tsx</automated>
  </verify>
  <acceptance_criteria>
    - __root.tsx has NavLink to="/how-it-works" in SiteHeader
    - __root.tsx has Link to="/how-it-works" in SiteFooter
    - Both use text "How it works" per D-03
    - Header link has a Lucide icon matching the NavLink pattern
    - Passes ultracite check
  </acceptance_criteria>
  <done>Header and footer both contain "How it works" navigation links</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of How It Works page</name>
  <files>apps/web/src/routes/how-it-works.tsx, apps/web/src/routes/__root.tsx</files>
  <read_first>
    - apps/web/src/routes/how-it-works.tsx (the page being verified)
    - apps/web/src/routes/__root.tsx (nav links being verified)
  </read_first>
  <action>
Start the dev server if not running (`cd apps/web && pnpm dev`), then present the verification checklist to the user for visual confirmation of the complete How It Works page.
  </action>
  <what-built>Complete "How It Works" explainer page with 5 sections (hero, data sources, walkthrough, AI transparency, FAQ) and navigation links in header/footer</what-built>
  <how-to-verify>
    1. Run the dev server: `cd apps/web && pnpm dev`
    2. Visit http://localhost:3000/how-it-works
    3. Verify header shows "How it works" link with active styling
    4. Scroll through all sections:
       - Hero: clear heading and subtitle
       - Data Sources: 5 cards with icons, descriptions, and working .gc.ca links
       - Walkthrough: 3 numbered steps readable left-to-right on desktop
       - AI Transparency: two columns (does/doesn't), 3 confidence tier examples
       - FAQ: accordion items open/close on click, all 5 questions present
    5. Toggle dark mode — all sections should look correct
    6. Resize to mobile width — layout should stack to single column
    7. Click footer "How it works" link — should navigate to the page
    8. Navigate to / and verify header link navigates correctly
  </how-to-verify>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && grep -c "how-it-works" apps/web/src/routes/__root.tsx</automated>
  </verify>
  <acceptance_criteria>
    - User confirms page renders correctly in both light and dark mode
    - User confirms all 5 sections are visible and readable
    - User confirms FAQ accordion items expand/collapse
    - User confirms navigation links work from header and footer
    - User confirms mobile responsive layout is acceptable
  </acceptance_criteria>
  <done>User has approved the visual appearance and functionality of the How It Works page</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- Header nav contains "How it works" NavLink
- Footer contains "How it works" Link
- Page renders correctly at /how-it-works in both light and dark mode
- All FAQ accordion items expand/collapse
- All external links open in new tabs
- Mobile responsive layout works
</verification>

<success_criteria>
User confirms the How It Works page looks correct, reads well, and navigation works from both header and footer.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-how-it-works/04.1-02-SUMMARY.md`
</output>
