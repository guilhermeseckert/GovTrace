---
phase: 07-parliamentary-voting-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/parliament.ts
  - packages/db/src/schema/raw.ts
  - apps/ingestion/src/parsers/parliament-votes.ts
  - apps/ingestion/src/parsers/parliament-votes.test.ts
  - apps/ingestion/src/parsers/parliament-ballots.ts
  - apps/ingestion/src/parsers/parliament-ballots.test.ts
  - apps/ingestion/src/parsers/parliament-bills.ts
  - apps/ingestion/src/parsers/parliament-bills.test.ts
  - apps/ingestion/src/parsers/__fixtures__/parliament-votes-sample.xml
  - apps/ingestion/src/parsers/__fixtures__/parliament-ballots-sample.xml
  - apps/ingestion/src/parsers/__fixtures__/parliament-bills-sample.json
  - apps/ingestion/src/downloaders/parliament.ts
  - apps/ingestion/src/upsert/parliament-votes.ts
  - apps/ingestion/src/upsert/parliament-ballots.ts
  - apps/ingestion/src/upsert/parliament-bills.ts
  - apps/ingestion/src/runners/parliament.ts
  - apps/ingestion/src/matcher/run-matching.ts
  - apps/ingestion/src/graph/build-connections.ts
  - apps/ingestion/src/scheduler/jobs.ts
  - apps/ingestion/src/index.ts
autonomous: true
requirements: [PARL-01, PARL-05]

must_haves:
  truths:
    - "All parliament sessions from 38th to 45th have aggregate vote records ingested"
    - "Individual MP ballot records are stored per division with PersonId linkage"
    - "Bills from LEGISinfo are ingested with short/long titles and status"
    - "MP PersonIds are matched to existing politician entities via mp_profiles"
    - "Bill summaries are generated via Claude Haiku for grandpa-readable explanations"
    - "Parliamentary data is wired into the entity matching pipeline and entity_connections graph"
  artifacts:
    - path: "packages/db/src/schema/parliament.ts"
      provides: "5 new tables: parliamentBills, parliamentVotes, parliamentVoteBallots, mpProfiles, billSummaries"
      exports: ["parliamentBills", "parliamentVotes", "parliamentVoteBallots", "mpProfiles", "billSummaries"]
    - path: "apps/ingestion/src/parsers/parliament-votes.ts"
      provides: "XML parser for ArrayOfVote aggregate listings"
      exports: ["parseVotesXml"]
    - path: "apps/ingestion/src/parsers/parliament-ballots.ts"
      provides: "XML parser for ArrayOfVoteParticipant per-division ballots"
      exports: ["parseVoteBallotsXml"]
    - path: "apps/ingestion/src/parsers/parliament-bills.ts"
      provides: "JSON parser for LEGISinfo bill listings"
      exports: ["parseBillsJson"]
    - path: "apps/ingestion/src/runners/parliament.ts"
      provides: "Orchestrator: sessions loop -> bills -> votes -> ballots -> entity matching -> bill summaries"
      exports: ["runParliamentIngestion"]
  key_links:
    - from: "apps/ingestion/src/runners/parliament.ts"
      to: "packages/db/src/schema/parliament.ts"
      via: "drizzle insert/upsert"
      pattern: "parliamentVotes|parliamentBills|parliamentVoteBallots|mpProfiles"
    - from: "apps/ingestion/src/matcher/run-matching.ts"
      to: "packages/db/src/schema/parliament.ts"
      via: "SOURCE_CONFIGS entry for mp_profiles"
      pattern: "mp_profiles"
    - from: "apps/ingestion/src/graph/build-connections.ts"
      to: "packages/db/src/schema/parliament.ts"
      via: "SQL join for mp_voted_yea/nay_on_bill connections"
      pattern: "mp_voted_.*_on_bill"
---

<objective>
Build the complete parliamentary voting records ingestion pipeline: 5 new database tables, XML/JSON parsers for ourcommons.ca votes and LEGISinfo bills, MP entity matching via PersonId-anchored mp_profiles, bill AI summary generation, and wiring into the existing entity matching and entity_connections graph.

Purpose: PARL-01 requires all House of Commons voting records from 2004+ ingested and linked to entities. PARL-05 requires AI-generated bill summaries. This plan builds the full data layer.
Output: Runnable `pnpm --filter @govtrace/ingestion start parliament` pipeline that ingests all historical voting data.
</objective>

<execution_context>
@/Users/geckert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parliamentary-voting-records/07-RESEARCH.md
@.planning/phases/05-international-aid-ingestion/05-01-SUMMARY.md

<interfaces>
<!-- Existing ingestion patterns to follow -->

From apps/ingestion/src/parsers/international-aid.ts:
```typescript
import { XMLParser } from 'fast-xml-parser'
// isArray callback pattern for XML array forcing
const parser = new XMLParser({
  ignoreAttributes: false,
  attributeNamePrefix: '@_',
  isArray: (name) => ['iati-activity', 'budget', 'transaction', ...].includes(name),
  parseAttributeValue: false,
  trimValues: true,
})
export function parseIatiFile(filePath: string, sourceFileHash: string): IatiRecord[]
```

From apps/ingestion/src/matcher/run-matching.ts:
```typescript
type SourceConfig = { table: string; nameField: string; normalizedField: string; entityIdField: string }
const SOURCE_CONFIGS: SourceConfig[] = [
  { table: 'donations', nameField: 'contributor_name', ... },
  // ... 7 more entries
]
export async function runMatchingPipeline(): Promise<MatchingStats>
```

From apps/ingestion/src/graph/build-connections.ts:
```typescript
// Adds connection types like 'aid_recipient_to_department' via raw SQL
// Pattern: db.execute(sql`INSERT INTO entity_connections ...`)
```

From apps/ingestion/src/scheduler/jobs.ts:
```typescript
// Pattern: job name constant + worker function + schedule registration
// Monthly schedule for international aid: '30 5 1-7 * 0'
```

From apps/ingestion/src/index.ts:
```typescript
// CLI switch: case 'international-aid': await runInternationalAidIngestion()
```

From packages/db/src/schema/raw.ts:
```typescript
// All raw source tables defined here with pgTable(), indexes, and JSONB rawData columns
// internationalAid table is the most recent addition
```

From apps/ingestion/src/matcher/deterministic.ts:
```typescript
export async function findDeterministicMatch(name: string, sourceTable: string, sourceField: string): Promise<DeterministicMatchResult | null>
```

From apps/ingestion/src/matcher/fuzzy.ts:
```typescript
export async function findFuzzyMatches(name: string): Promise<FuzzyMatchResult[]>
```

From apps/ingestion/src/matcher/ai-verify.ts:
```typescript
export async function verifyMatchWithAI(sourceName: string, candidateName: string, sourceContext: string, candidateContext: string): Promise<AIVerifyResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + parsers + test fixtures</name>
  <files>
    packages/db/src/schema/parliament.ts
    apps/ingestion/src/parsers/parliament-votes.ts
    apps/ingestion/src/parsers/parliament-votes.test.ts
    apps/ingestion/src/parsers/parliament-ballots.ts
    apps/ingestion/src/parsers/parliament-ballots.test.ts
    apps/ingestion/src/parsers/parliament-bills.ts
    apps/ingestion/src/parsers/parliament-bills.test.ts
    apps/ingestion/src/parsers/__fixtures__/parliament-votes-sample.xml
    apps/ingestion/src/parsers/__fixtures__/parliament-ballots-sample.xml
    apps/ingestion/src/parsers/__fixtures__/parliament-bills-sample.json
  </files>
  <behavior>
    - parseVotesXml: parses ArrayOfVote XML with multiple Vote elements into VoteRecord[] with all fields (parliamentNumber, sessionNumber, divisionNumber, subject, resultName, yeas/nays/paired, billNumberCode nullable)
    - parseVotesXml: handles single-Vote XML (isArray config prevents object collapse) — returns array with 1 element
    - parseVotesXml: returns empty array for empty/malformed XML
    - parseVoteBallotsXml: parses ArrayOfVoteParticipant XML into BallotRecord[] with personId, firstName, lastName, constituency, province, caucusShortName, ballotValue, isYea/isNay/isPaired booleans
    - parseVoteBallotsXml: handles single-VoteParticipant XML without crashing
    - parseBillsJson: parses LEGISinfo JSON array into BillRecord[] with billId, billNumberFormatted, shortTitleEn, longTitleEn, billTypeEn, sponsorEn, currentStatusEn, royalAssentDate (nullable), parlSessionCode
    - parseBillsJson: handles bills with null ShortTitleEn and null ReceivedRoyalAssentDateTime
  </behavior>
  <action>
    1. Create `packages/db/src/schema/parliament.ts` with 5 tables exactly as specified in 07-RESEARCH.md "Database Schema" section:
       - `parliamentBills` — PK format "{parliament}-{session}-{BillNumberFormatted}", pg_trgm GIN index on billNumberFormatted for bill search (PARL-03)
       - `parliamentVotes` — PK format "{parliament}-{session}-{divisionNumber}", unique index on (parlSessionCode, divisionNumber), nullable billId FK to parliamentBills
       - `parliamentVoteBallots` — PK format "{voteId}-{personId}", indexes on voteId, entityId, personId, caucusShortName, ballotValue
       - `mpProfiles` — PK is personId (integer), entityId nullable UUID, matchMethod/matchConfidence for provenance
       - `billSummaries` — UUID PK, billId FK with cascade delete, unique index on billId, model field for claude-haiku-3-5

    2. Export all 5 tables from parliament.ts. Do NOT re-export from raw.ts — parliament.ts is its own schema file (same level as raw.ts, entities.ts, etc).

    3. Create XML test fixtures by extracting realistic samples from 07-RESEARCH.md examples:
       - `parliament-votes-sample.xml`: ArrayOfVote with 3 Vote elements — one with BillNumberCode "C-69", one with empty BillNumberCode (motion), one paired vote
       - `parliament-ballots-sample.xml`: ArrayOfVoteParticipant with 4 VoteParticipant elements — Yea, Nay, Paired, and one with French-accented name
       - `parliament-bills-sample.json`: JSON array with 3 bills — one with full data, one with null ShortTitleEn, one with null RoyalAssentDateTime

    4. Create parsers following the fast-xml-parser pattern from international-aid.ts:
       - `parliament-votes.ts`: `parseVotesXml(xml: string, parlSessionCode: string): VoteRecord[]` — isArray config for ['Vote'], extracts all fields per research schema
       - `parliament-ballots.ts`: `parseVoteBallotsXml(xml: string, voteId: string): BallotRecord[]` — isArray config for ['VoteParticipant'], extracts PersonId and all ballot fields
       - `parliament-bills.ts`: `parseBillsJson(json: string, parlSessionCode: string): BillRecord[]` — JSON.parse, map fields, construct bill ID as "{parliament}-{session}-{billNumberFormatted}"

    5. Write tests (TDD: write tests first, then implement parsers):
       - `parliament-votes.test.ts`: 3 tests — multi-vote parsing, single-vote array safety, empty/malformed XML
       - `parliament-ballots.test.ts`: 3 tests — multi-participant parsing, single-participant array safety, French accent handling
       - `parliament-bills.test.ts`: 3 tests — full bill parsing, null fields handling, bill ID construction

    Export typed interfaces for VoteRecord, BallotRecord, BillRecord from each parser file.
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && pnpm --filter @govtrace/ingestion test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>5 tables defined in parliament.ts with all indexes. 3 parsers pass all tests. Fixtures contain realistic XML/JSON samples covering edge cases (single-element arrays, nullable fields, French characters).</done>
</task>

<task type="auto">
  <name>Task 2: Downloaders + upsert + MP entity matching</name>
  <files>
    apps/ingestion/src/downloaders/parliament.ts
    apps/ingestion/src/upsert/parliament-votes.ts
    apps/ingestion/src/upsert/parliament-ballots.ts
    apps/ingestion/src/upsert/parliament-bills.ts
  </files>
  <action>
    1. Create `apps/ingestion/src/downloaders/parliament.ts` with 4 download functions:
       - `fetchVotesXml(parliament: number, session: number): Promise<string>` — GET `https://www.ourcommons.ca/members/en/votes/xml?parlSession={parliament}-{session}`, return raw XML text. Throw on non-200.
       - `fetchBallotsXml(parliament: number, session: number, divisionNumber: number): Promise<string>` — GET `https://www.ourcommons.ca/members/en/votes/{parliament}/{session}/{divisionNumber}/xml`, return raw XML text.
       - `fetchMembersXml(parliament: number, session: number): Promise<string>` — GET `https://www.ourcommons.ca/members/en/search/xml?parlSession={parliament}-{session}`, return raw XML text.
       - `fetchBillsJson(parliament: number, session: number): Promise<string>` — GET `https://www.parl.ca/legisinfo/en/bills/json?parlsession={parliament}-{session}&Language=E`, return raw JSON text.

       Export a `PARLIAMENT_SESSIONS` const array with all 16 sessions from 37-1 through 45-1 (see research "Parliament Sessions Reference"). Mark 37th Parliament sessions with a `confirmed: false` flag so the runner can skip on HTTP error.

    2. Create `apps/ingestion/src/upsert/parliament-bills.ts`:
       - `upsertBills(bills: BillRecord[]): Promise<number>` — INSERT ON CONFLICT DO UPDATE on bill PK. Update shortTitleEn, longTitleEn, currentStatusEn, receivedRoyalAssentAt, rawData, updatedAt on conflict. BATCH_SIZE=500. Return count inserted/updated.

    3. Create `apps/ingestion/src/upsert/parliament-votes.ts`:
       - `upsertVotes(votes: VoteRecord[], sourceFileHash: string): Promise<number>` — INSERT ON CONFLICT DO UPDATE using the unique index on (parlSessionCode, divisionNumber). Link billId by looking up `"{parliament}-{session}-{billNumberCode}"` in parliamentBills when BillNumberCode is non-empty (Pitfall 4 from research: many votes have no bill). BATCH_SIZE=500.

    4. Create `apps/ingestion/src/upsert/parliament-ballots.ts`:
       - `upsertBallots(ballots: BallotRecord[], entityLookup: Map<number, string>): Promise<number>` — INSERT ON CONFLICT DO NOTHING (ballots are immutable). Set entityId from the entityLookup map keyed by personId. BATCH_SIZE=1000 (ballots are narrow rows). Return count inserted.
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && npx tsc --noEmit --project apps/ingestion/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>4 download functions fetch from government endpoints. 3 upsert functions handle conflict resolution. Bills upsert updates on conflict, votes link to bills when BillNumberCode exists, ballots use DO NOTHING with entityId from PersonId lookup map.</done>
</task>

<task type="auto">
  <name>Task 3: Runner + entity matching + scheduler + bill summaries</name>
  <files>
    apps/ingestion/src/runners/parliament.ts
    apps/ingestion/src/matcher/run-matching.ts
    apps/ingestion/src/graph/build-connections.ts
    apps/ingestion/src/scheduler/jobs.ts
    apps/ingestion/src/index.ts
  </files>
  <action>
    1. Create `apps/ingestion/src/runners/parliament.ts` — `runParliamentIngestion()` orchestrator:

       **Phase A — Bills (LEGISinfo JSON):** For each session in PARLIAMENT_SESSIONS, fetch bills JSON, parse with parseBillsJson, upsert. Skip sessions where fetch returns error (37th Parliament). Log count per session.

       **Phase B — Aggregate votes:** For each session, fetch votes XML, parse with parseVotesXml, upsert (links to bills via BillNumberCode). Log count per session.

       **Phase C — MP entity matching:** For each session, fetch members XML, parse into MP records. For each MP:
         - Check mp_profiles cache by personId — if entityId exists, skip
         - Run deterministic match against entities table (fullName = firstName + " " + lastName)
         - If no match: run fuzzy match (pg_trgm similarity > 0.60)
         - If medium-confidence fuzzy: run AI verification via existing verifyMatchWithAI
         - If no match at all: create new entity with entityType='politician', province from constituency
         - Upsert mp_profiles with personId, entityId, matchMethod, matchConfidence
       Build entityLookup Map<number, string> (personId -> entityId) from mp_profiles after matching.

       **Phase D — Ballots:** For each session, get list of division numbers from parliamentVotes that have no ballots yet (resumable). For each division, fetch ballots XML, parse with parseVoteBallotsXml, upsert with entityLookup. Add 100ms delay between requests (courteous to government servers, per research Pitfall 2). Log progress every 100 divisions.

       **Phase E — Bill summaries (PARL-05):** Query parliamentBills that have no entry in billSummaries. For each bill, call Claude Haiku with the prompt template from research "Bill Summary Claude Prompt" section. Use `claude-haiku-3-5` model. Insert into billSummaries table. Process in batches of 50 with 200ms delay. Log count. Circuit breaker: if more than 5000 bills need summaries, log warning and proceed (cost is ~$1 total per research estimate, well within budget).

       Log ingestion_runs record with source='parliament', stats in rawData.

    2. Modify `apps/ingestion/src/matcher/run-matching.ts` — this is NOT needed for the PersonId-based MP matching (which is custom in the runner). However, add a note comment explaining that parliamentary MP matching uses PersonId-anchored mp_profiles instead of the generic SOURCE_CONFIGS pipeline.

    3. Modify `apps/ingestion/src/graph/build-connections.ts` — add a new SQL block for `mp_voted_on_bill` connections:
       ```sql
       INSERT INTO entity_connections (id, entity_a_id, entity_a_type, entity_b_id, entity_b_type, connection_type, total_value, connection_count, first_seen, last_seen, metadata)
       SELECT
         gen_random_uuid(),
         mp.entity_id,
         'politician',
         bill_entity.id,  -- need to handle: bills don't have entities. Use metadata instead.
         ...
       ```
       Actually, bills are not entities — they don't have an entity_id. The cross-reference (PARL-04) is done at query time: "MP voted Yea on bill X about energy" + "MP received donations from oil company Y". So entity_connections is NOT the right place for vote-to-bill links. Instead, the cross-reference happens in the AI summary prompt (Plan 02).

       DO NOT add mp_voted_yea/nay_on_bill to entity_connections. Bills are not entities. The cross-reference is computed at AI summary generation time by querying vote_ballots + donations together.

    4. Modify `apps/ingestion/src/scheduler/jobs.ts` — add `INGEST_PARLIAMENT` job:
       - Job name: 'ingest-parliament'
       - Worker calls runParliamentIngestion()
       - Schedule: weekly Sunday 2am UTC ('0 2 * * 0') — after elections-canada (midnight) and before build-connections (8am)

    5. Modify `apps/ingestion/src/index.ts` — add CLI case:
       ```typescript
       case 'parliament':
         await runParliamentIngestion()
         break
       ```
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && npx tsc --noEmit --project apps/ingestion/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Runner orchestrates 5-phase ingestion (bills -> votes -> MP matching -> ballots -> bill summaries). Scheduler runs weekly Sunday 2am. CLI accepts `parliament` command. MP matching uses PersonId-anchored mp_profiles with deterministic/fuzzy/AI fallback. Bill summaries generated via Claude Haiku. No entity_connections changes (bills are not entities — cross-reference is query-time).</done>
</task>

</tasks>

<verification>
1. All parser tests pass: `pnpm --filter @govtrace/ingestion test -- --run`
2. TypeScript compiles without new errors: `npx tsc --noEmit --project apps/ingestion/tsconfig.json`
3. Schema file exports 5 tables: `grep -c 'export const' packages/db/src/schema/parliament.ts` returns 5
4. Runner exports runParliamentIngestion: `grep 'runParliamentIngestion' apps/ingestion/src/runners/parliament.ts`
5. Scheduler has parliament job: `grep 'ingest-parliament' apps/ingestion/src/scheduler/jobs.ts`
6. CLI has parliament case: `grep "'parliament'" apps/ingestion/src/index.ts`
</verification>

<success_criteria>
- 5 new database tables defined with correct PKs, FKs, and indexes per research schema
- 3 parsers handle XML/JSON with isArray safety, nullable fields, and French characters
- All parser tests pass
- Runner orchestrates bills -> votes -> MP matching -> ballots -> bill summaries in sequence
- MP matching uses PersonId as stable anchor, falls back to deterministic/fuzzy/AI
- Bill summaries generated via Claude Haiku with grandpa-readable prompt
- Scheduler registers weekly parliament ingestion job
- CLI `parliament` command added
</success_criteria>

<output>
After completion, create `.planning/phases/07-parliamentary-voting-records/07-01-SUMMARY.md`
</output>
