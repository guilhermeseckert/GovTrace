---
phase: 06-debt-vs-spending-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/src/routes/dashboard.tsx
  - apps/web/src/components/dashboard/DebtHeroStats.tsx
  - apps/web/src/components/dashboard/DebtVsAidChart.tsx
  - apps/web/src/components/dashboard/DepartmentBreakdown.tsx
autonomous: false
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04

must_haves:
  truths:
    - "User can navigate to /dashboard and see the national debt vs aid spending page"
    - "User sees hero stat cards showing current debt, total aid, and the aid-as-percent-of-debt ratio"
    - "User sees a time-series chart with debt and aid lines plus election year markers"
    - "User sees a department breakdown showing which departments authorize the most spending"
    - "Every number has a source link to the original government data"
  artifacts:
    - path: "apps/web/src/routes/dashboard.tsx"
      provides: "Dashboard page route with SSR data loading"
      min_lines: 30
    - path: "apps/web/src/components/dashboard/DebtVsAidChart.tsx"
      provides: "D3-powered dual-series line chart with election markers"
      min_lines: 80
    - path: "apps/web/src/components/dashboard/DepartmentBreakdown.tsx"
      provides: "Ranked department spending list with percentage bars"
      min_lines: 40
    - path: "apps/web/src/components/dashboard/DebtHeroStats.tsx"
      provides: "Big number cards for debt, aid, and ratio"
      min_lines: 30
  key_links:
    - from: "apps/web/src/routes/dashboard.tsx"
      to: "apps/web/src/server-fns/dashboard.ts"
      via: "loader calling getDebtTimeline, getDepartmentBreakdown, getDebtHeroStats"
      pattern: "getDebtTimeline|getDepartmentBreakdown|getDebtHeroStats"
    - from: "apps/web/src/components/dashboard/DebtVsAidChart.tsx"
      to: "apps/web/src/lib/constants/elections.ts"
      via: "import FEDERAL_ELECTION_DATES for vertical marker lines"
      pattern: "FEDERAL_ELECTION_DATES"
    - from: "apps/web/src/components/dashboard/DebtVsAidChart.tsx"
      to: "d3"
      via: "D3 scales and line generators (math only, React renders SVG)"
      pattern: "d3\\.scaleLinear|d3\\.scaleTime|d3\\.line"
---

<objective>
Build the dashboard page at /dashboard with three visual sections: hero stat cards (debt, aid, ratio), a D3 dual-series time-series chart with election markers, and a department spending breakdown.

Purpose: This is the user-facing page that tells the civic story — how much Canada sends overseas relative to the national debt, contextualized by election cycles.
Output: A working /dashboard route with responsive D3 chart, hero stats, and department breakdown.
</objective>

<execution_context>
@/Users/geckert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-debt-vs-spending-dashboard/06-RESEARCH.md
@.planning/phases/06-debt-vs-spending-dashboard/06-01-SUMMARY.md

@apps/web/src/components/visualizations/NetworkGraph.tsx
@apps/web/src/components/visualizations/shared/useResizeObserver.ts
@apps/web/src/components/visualizations/MoneyFlowSankey.tsx
@apps/web/src/routes/how-it-works.tsx

<interfaces>
<!-- From Plan 01 — server function types the executor needs -->

From apps/web/src/server-fns/dashboard.ts:
```typescript
export type DebtAidDataPoint = {
  year: number
  debtBillionsCad: number
  aidCommittedBillionsCad: number
  aidDisbursedBillionsCad: number
  sourceDebtUrl: string
  sourceAidUrl: string
}

export type DeptSpendingRow = {
  department: string
  totalCommittedCad: number
  projectCount: number
  pctOfTotal: number
}

// Hero stats return shape
export type DebtHeroStats = {
  currentDebtBillions: number
  totalAidBillions: number
  aidAsPercentOfDebt: number
  debtAsOf: string
  sourceDebtUrl: string
  sourceAidUrl: string
}

export const getDebtTimeline: ServerFn  // GET
export const getDepartmentBreakdown: ServerFn  // GET
export const getDebtHeroStats: ServerFn  // GET
```

From apps/web/src/lib/constants/elections.ts:
```typescript
export const FEDERAL_ELECTION_DATES: readonly {
  election: string; date: string; year: number; winner: string
}[]
```

From apps/web/src/components/visualizations/shared/useResizeObserver.ts:
```typescript
export function useResizeObserver(ref: RefObject<HTMLElement>): { width: number; height: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard route + all three visual components</name>
  <files>
    apps/web/src/routes/dashboard.tsx,
    apps/web/src/components/dashboard/DebtHeroStats.tsx,
    apps/web/src/components/dashboard/DebtVsAidChart.tsx,
    apps/web/src/components/dashboard/DepartmentBreakdown.tsx
  </files>
  <action>
    1. **Route** — Create `apps/web/src/routes/dashboard.tsx` (DEBT-01):
       - `createFileRoute('/dashboard')` with a loader that calls all three server functions in `Promise.all`: `getDebtTimeline()`, `getDepartmentBreakdown()`, `getDebtHeroStats()`
       - Page layout: full-width container with `max-w-6xl mx-auto` padding
       - Title: "Debt vs Overseas Spending" with subtitle "How much does Canada send overseas relative to the national debt?"
       - Render three sections vertically: `<DebtHeroStats>`, `<DebtVsAidChart>`, `<DepartmentBreakdown>`
       - Add footnotes section at bottom with data caveats (per Research Pitfall 3): "Aid figures show project start year; Government of Canada uses April-March fiscal year" and "Source: Statistics Canada Table 10-10-0002-01, Global Affairs Canada IATI Activity Files"
       - Add a nav link to the dashboard in the app's navigation (wherever the existing nav links are defined — check `__root.tsx` or the layout component)

    2. **DebtHeroStats** — Create `apps/web/src/components/dashboard/DebtHeroStats.tsx` (DEBT-01, DEBT-04):
       - Accept `DebtHeroStats` typed props from the server function
       - Three card layout using shadcn/ui Card component in a responsive grid (`grid-cols-1 md:grid-cols-3 gap-4`)
       - Card 1: "National Debt" — format `currentDebtBillions` as "$X.X trillion" or "$X.X billion" with `debtAsOf` date below
       - Card 2: "Overseas Aid" — format `totalAidBillions` as "$X.X billion" with "total committed" label
       - Card 3: "Aid as % of Debt" — format `aidAsPercentOfDebt` with one decimal (e.g., "0.7%") — this is the "ratio story" (Research recommendation for grandpa-friendly UX)
       - Each card has an ExternalLink icon linking to the source URL (DEBT-04) — use `rel="noopener"` with `target="_blank"`

    3. **DebtVsAidChart** — Create `apps/web/src/components/dashboard/DebtVsAidChart.tsx` (DEBT-02):
       - Accept `data: DebtAidDataPoint[]` props
       - Follow the "D3 for math, React for rendering" pattern from `NetworkGraph.tsx` — NO `d3.select` or `.append()` calls
       - Use `useResizeObserver` from `@/components/visualizations/shared/useResizeObserver` for responsive width
       - D3 scales:
         - `xScale`: `d3.scaleTime()` with domain from min year to max year in data
         - `yDebt` (left axis): `d3.scaleLinear()` for debt in billions, `.nice()`
         - `yAid` (right axis): `d3.scaleLinear()` for aid in billions, `.nice()`
       - D3 line generators:
         - `debtLine`: `d3.line()` using xScale + yDebt, `curveMonotoneX`
         - `aidLine`: `d3.line()` using xScale + yAid, `curveMonotoneX`
       - Render as React SVG elements:
         - `<path>` for debt line (use `stroke="var(--destructive)"`, strokeWidth 2.5)
         - `<path>` for aid line (use `stroke="var(--chart-2)"`, strokeWidth 2.5)
         - Y-axis labels: left "National Debt ($ billions)" and right "Aid Committed ($ billions)"
         - X-axis tick labels for years (use `d3.timeYear.every(2)` or similar, guard for undefined)
         - Y-axis tick marks rendered as `<text>` + `<line>` grid lines
       - **Election markers** (DEBT-02): Import `FEDERAL_ELECTION_DATES` from `@/lib/constants/elections`. For each election within the chart's x-domain, render a dashed vertical `<line>` from top to bottom margin (`strokeDasharray="4,2"`, `stroke="var(--muted-foreground)"`, strokeWidth 1). Add a small rotated `<text>` label at the top showing the year.
       - **Legend**: Small inline legend below chart showing debt line color + label and aid line color + label, plus dashed line for election markers
       - Margin: `{ top: 30, right: 70, bottom: 50, left: 80 }` (right margin larger for second y-axis labels)
       - Height: 400px fixed
       - Accessible: `role="img"` and `aria-label="National debt versus overseas aid spending over time"` on the `<svg>`
       - Add annotation text near the chart: "Note: Dual y-axes — debt and aid are on different scales" to address Research Pitfall 2

    4. **DepartmentBreakdown** — Create `apps/web/src/components/dashboard/DepartmentBreakdown.tsx` (DEBT-03, DEBT-04):
       - Accept `data: DeptSpendingRow[]` props
       - Section title: "Spending by Department"
       - Render as a list/table of departments with CSS percentage bars (NOT D3 — follow MoneyFlowSankey.tsx pattern)
       - Each row shows: department name, total committed formatted as currency ("$X.XB" or "$X.XM"), project count, and a percentage bar using `style={{ width: \`${pctOfTotal}%\` }}` relative to the max department
       - Source link at bottom: "Source: Global Affairs Canada IATI Activity Files" with ExternalLink icon linking to `https://open.canada.ca/data/en/dataset/2f7e22f0-88f6-430c-9723-547043f898ad` (DEBT-04)
       - Limit display to top 10 departments; if more exist, show a "and X more departments" note
  </action>
  <verify>
    <automated>cd /Users/geckert/Documents/GovTrace && pnpm tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -10 && pnpm --filter @govtrace/web build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - /dashboard route exists and loads data via server functions in the loader
    - Hero stats render three cards with formatted debt, aid, and ratio numbers
    - D3 line chart renders dual-series with election vertical markers
    - Department breakdown renders ranked list with percentage bars
    - All numbers have source links (ExternalLink icons) to government data
    - TypeScript compiles and build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard page</name>
  <files>apps/web/src/routes/dashboard.tsx</files>
  <action>
    Human verifies the complete dashboard page renders correctly with all three sections.
    What was built: Complete Debt vs Spending Dashboard at /dashboard with hero stats, D3 chart, and department breakdown.
  </action>
  <verify>
    How to verify:
    1. Start dev server: `cd /Users/geckert/Documents/GovTrace && pnpm --filter @govtrace/web dev`
    2. Navigate to `http://localhost:3000/dashboard`
    3. Verify hero stat cards show national debt, total aid, and aid-as-percent-of-debt
    4. Verify the time-series chart shows two lines (debt in red, aid in a second color) with dashed vertical election markers
    5. Verify chart has labeled y-axes on both sides and year labels on x-axis
    6. Verify department breakdown shows ranked departments with percentage bars
    7. Verify source links (ExternalLink icons) on hero cards and department section open government data pages
    8. Resize the browser window — chart should resize responsively
    9. Check the nav — dashboard link should appear in the site navigation
    10. Check footnotes at page bottom for data caveats

    Note: If fiscal_snapshots table has no data yet (ingestion hasn't been run), the chart and hero stats may show empty/zero values. That is expected — the ingestion runner from Plan 01 needs to be executed against the production database to populate data.
  </verify>
  <done>User confirms dashboard page looks correct and all sections render properly, or describes issues to fix.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @govtrace/web build` succeeds
- `/dashboard` route is accessible in dev mode
- Chart renders without D3 DOM manipulation errors
- All source links point to valid government URLs
</verification>

<success_criteria>
- Dashboard page loads at /dashboard with all three sections visible
- Hero stats display formatted debt/aid/ratio with source links
- D3 chart shows dual-series lines with election markers and dual y-axes
- Department breakdown shows ranked list with percentage bars
- Navigation includes a link to the dashboard
- Page includes data source footnotes
</success_criteria>

<output>
After completion, create `.planning/phases/06-debt-vs-spending-dashboard/06-02-SUMMARY.md`
</output>
